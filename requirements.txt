# Python dependencies for the deep survival model and figure production.
# Tested with Python 3.10.17
# Install with:  python -m pip install -r requirements.txt
numpy>=1.24
pandas>=2.0
torch==2.7.1
matplotlib>=3.7
openpyxl>=3.1
